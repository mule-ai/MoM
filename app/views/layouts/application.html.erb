<!DOCTYPE html>
<html>
  <head>
    <title>MoM - Mule of Mules</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <style>
      :root {
        --bg-primary: #f8f9fa;
        --bg-secondary: #ffffff;
        --bg-header: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        --text-primary: #333333;
        --text-secondary: #666666;
        --text-header: #ffffff;
        --border-color: #e9ecef;
        --shadow: rgba(0,0,0,0.1);
        --btn-primary: #007bff;
        --btn-primary-hover: #0056b3;
        --btn-danger: #dc3545;
        --btn-danger-hover: #c82333;
        --btn-success: #28a745;
        --btn-success-hover: #218838;
        --status-active-bg: #d4edda;
        --status-active-text: #155724;
        --status-inactive-bg: #f8d7da;
        --status-inactive-text: #721c24;
        --status-idle-bg: #fff3cd;
        --status-idle-text: #856404;
        --alert-success-bg: #d4edda;
        --alert-success-text: #155724;
        --alert-success-border: #c3e6cb;
        --alert-error-bg: #f8d7da;
        --alert-error-text: #721c24;
        --alert-error-border: #f5c6cb;
        --form-border: #ced4da;
        --card-header-bg: #f8f9fa;
      }

      @media (prefers-color-scheme: dark) {
        :root {
          --bg-primary: #1a1a1a;
          --bg-secondary: #2d2d2d;
          --bg-header: linear-gradient(135deg, #4a5568 0%, #553c9a 100%);
          --text-primary: #e2e8f0;
          --text-secondary: #a0aec0;
          --text-header: #ffffff;
          --border-color: #4a5568;
          --shadow: rgba(0,0,0,0.3);
          --btn-primary: #3182ce;
          --btn-primary-hover: #2c5aa0;
          --btn-danger: #e53e3e;
          --btn-danger-hover: #c53030;
          --btn-success: #38a169;
          --btn-success-hover: #2f855a;
          --status-active-bg: #2d5a3d;
          --status-active-text: #68d391;
          --status-inactive-bg: #5a2d3d;
          --status-inactive-text: #fc8181;
          --status-idle-bg: #5a4d2d;
          --status-idle-text: #f6e05e;
          --alert-success-bg: #2d5a3d;
          --alert-success-text: #68d391;
          --alert-success-border: #38a169;
          --alert-error-bg: #5a2d3d;
          --alert-error-text: #fc8181;
          --alert-error-border: #e53e3e;
          --form-border: #4a5568;
          --card-header-bg: #2d2d2d;
        }
      }

      [data-theme="light"] {
        --bg-primary: #f8f9fa;
        --bg-secondary: #ffffff;
        --bg-header: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        --text-primary: #333333;
        --text-secondary: #666666;
        --text-header: #ffffff;
        --border-color: #e9ecef;
        --shadow: rgba(0,0,0,0.1);
        --btn-primary: #007bff;
        --btn-primary-hover: #0056b3;
        --btn-danger: #dc3545;
        --btn-danger-hover: #c82333;
        --btn-success: #28a745;
        --btn-success-hover: #218838;
        --status-active-bg: #d4edda;
        --status-active-text: #155724;
        --status-inactive-bg: #f8d7da;
        --status-inactive-text: #721c24;
        --status-idle-bg: #fff3cd;
        --status-idle-text: #856404;
        --alert-success-bg: #d4edda;
        --alert-success-text: #155724;
        --alert-success-border: #c3e6cb;
        --alert-error-bg: #f8d7da;
        --alert-error-text: #721c24;
        --alert-error-border: #f5c6cb;
        --form-border: #ced4da;
        --card-header-bg: #f8f9fa;
      }

      [data-theme="dark"] {
        --bg-primary: #1a1a1a;
        --bg-secondary: #2d2d2d;
        --bg-header: linear-gradient(135deg, #4a5568 0%, #553c9a 100%);
        --text-primary: #e2e8f0;
        --text-secondary: #a0aec0;
        --text-header: #ffffff;
        --border-color: #4a5568;
        --shadow: rgba(0,0,0,0.3);
        --btn-primary: #3182ce;
        --btn-primary-hover: #2c5aa0;
        --btn-danger: #e53e3e;
        --btn-danger-hover: #c53030;
        --btn-success: #38a169;
        --btn-success-hover: #2f855a;
        --status-active-bg: #2d5a3d;
        --status-active-text: #68d391;
        --status-inactive-bg: #5a2d3d;
        --status-inactive-text: #fc8181;
        --status-idle-bg: #5a4d2d;
        --status-idle-text: #f6e05e;
        --alert-success-bg: #2d5a3d;
        --alert-success-text: #68d391;
        --alert-success-border: #38a169;
        --alert-error-bg: #5a2d3d;
        --alert-error-text: #fc8181;
        --alert-error-border: #e53e3e;
        --form-border: #4a5568;
        --card-header-bg: #2d2d2d;
      }

      body {
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', sans-serif;
        margin: 0;
        padding: 0;
        background-color: var(--bg-primary);
        color: var(--text-primary);
        transition: background-color 0.3s ease, color 0.3s ease;
      }
      
      .header {
        background: var(--bg-header);
        color: var(--text-header);
        padding: 2rem 0;
        box-shadow: 0 4px 20px rgba(0,0,0,0.1);
        position: relative;
        overflow: hidden;
      }

      .header::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="grid" width="10" height="10" patternUnits="userSpaceOnUse"><path d="M 10 0 L 0 0 0 10" fill="none" stroke="rgba(255,255,255,0.1)" stroke-width="0.5"/></pattern></defs><rect width="100" height="100" fill="url(%23grid)"/></svg>');
        opacity: 0.3;
      }
      
      .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 1rem;
        position: relative;
        z-index: 1;
      }

      .header-content {
        display: flex;
        align-items: center;
        gap: 1.5rem;
      }

      .header-icon {
        width: 60px;
        height: 60px;
        background: rgba(255,255,255,0.15);
        border-radius: 16px;
        display: flex;
        align-items: center;
        justify-content: center;
        backdrop-filter: blur(10px);
        border: 1px solid rgba(255,255,255,0.2);
        padding: 8px;
      }

      .header-icon img {
        width: 100%;
        height: 100%;
        object-fit: contain;
      }

      .header-text {
        flex: 1;
      }
      
      .header .brand-name {
        margin: 0;
        font-size: 3.2rem;
        font-weight: 800;
        background: linear-gradient(45deg, #ffffff 0%, rgba(255,255,255,0.9) 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        letter-spacing: -0.03em;
        line-height: 0.9;
      }

      .header .brand-subtitle {
        margin: 0.25rem 0 0 0;
        font-size: 1.1rem;
        font-weight: 300;
        opacity: 0.8;
        letter-spacing: 0.05em;
        text-transform: uppercase;
      }
      
      .header .tagline {
        margin: 0.75rem 0 0 0;
        opacity: 0.7;
        font-size: 1rem;
        font-weight: 400;
        font-style: italic;
      }

      .header-stats {
        display: flex;
        gap: 2rem;
        margin-top: 1.5rem;
      }

      .header-stat {
        text-align: center;
      }

      .header-stat-number {
        display: block;
        font-size: 1.8rem;
        font-weight: 700;
        line-height: 1;
      }

      .header-stat-label {
        font-size: 0.9rem;
        opacity: 0.8;
        margin-top: 0.25rem;
      }
      
      .main-content {
        padding: 2rem 0;
      }
      
      .card {
        background: var(--bg-secondary);
        border-radius: 8px;
        box-shadow: 0 2px 8px var(--shadow);
        margin-bottom: 1.5rem;
        overflow: hidden;
      }
      
      .card-header {
        background: var(--card-header-bg);
        padding: 1rem 1.5rem;
        border-bottom: 1px solid var(--border-color);
        font-weight: 600;
        font-size: 1.1rem;
      }
      
      .card-body {
        padding: 1.5rem;
      }
      
      .btn {
        display: inline-block;
        padding: 0.5rem 1rem;
        background: var(--btn-primary);
        color: white;
        text-decoration: none;
        border-radius: 4px;
        border: none;
        cursor: pointer;
        font-size: 0.9rem;
        transition: background-color 0.2s;
        vertical-align: top;
      }
      
      .btn:hover {
        background: var(--btn-primary-hover);
      }
      
      .btn-danger {
        background: var(--btn-danger);
      }
      
      .btn-danger:hover {
        background: var(--btn-danger-hover);
      }
      
      .btn-success {
        background: var(--btn-success);
      }
      
      .btn-success:hover {
        background: var(--btn-success-hover);
      }
      
      .status-badge {
        display: inline-block;
        padding: 0.25rem 0.5rem;
        border-radius: 4px;
        font-size: 0.8rem;
        font-weight: 500;
      }
      
      .status-active {
        background: var(--status-active-bg);
        color: var(--status-active-text);
      }
      
      .status-inactive {
        background: var(--status-inactive-bg);
        color: var(--status-inactive-text);
      }
      
      .status-idle {
        background: var(--status-idle-bg);
        color: var(--status-idle-text);
      }
      
      .grid {
        display: grid;
        gap: 1rem;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      }
      
      .form-group {
        margin-bottom: 1rem;
      }
      
      .form-group label {
        display: block;
        margin-bottom: 0.5rem;
        font-weight: 500;
      }
      
      .form-control {
        width: 100%;
        padding: 0.5rem;
        border: 1px solid var(--form-border);
        border-radius: 4px;
        font-size: 1rem;
        box-sizing: border-box;
        background: var(--bg-secondary);
        color: var(--text-primary);
      }
      
      .alert {
        padding: 0.75rem 1rem;
        margin-bottom: 1rem;
        border-radius: 4px;
      }
      
      .alert-success {
        background: var(--alert-success-bg);
        color: var(--alert-success-text);
        border: 1px solid var(--alert-success-border);
      }
      
      .alert-error {
        background: var(--alert-error-bg);
        color: var(--alert-error-text);
        border: 1px solid var(--alert-error-border);
      }
      
      .stats {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 1rem;
        margin-bottom: 2rem;
      }
      
      .stat-card {
        background: var(--bg-secondary);
        padding: 1.5rem;
        border-radius: 8px;
        box-shadow: 0 2px 4px var(--shadow);
        text-align: center;
      }
      
      .stat-number {
        font-size: 2rem;
        font-weight: bold;
        color: #667eea;
      }
      
      .stat-label {
        color: var(--text-secondary);
        font-size: 0.9rem;
        margin-top: 0.5rem;
      }
      
      .back-link {
        color: var(--btn-primary);
        text-decoration: none;
        margin-bottom: 1rem;
        display: inline-block;
      }
      
      .back-link:hover {
        text-decoration: underline;
      }

      .theme-toggle {
        position: absolute;
        top: 1rem;
        right: 1rem;
        background: var(--bg-secondary);
        border: 1px solid var(--border-color);
        border-radius: 50%;
        width: 50px;
        height: 50px;
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        font-size: 1.2rem;
        box-shadow: 0 2px 8px var(--shadow);
        transition: all 0.3s ease;
        z-index: 1000;
      }

      .theme-toggle:hover {
        transform: scale(1.1);
      }

      .button-group {
        display: flex;
        gap: 0.5rem;
        align-items: center;
        margin-top: 1rem;
      }

      .button-group .btn {
        margin: 0;
      }
    </style>

    <script>
      // Theme management
      function initTheme() {
        const savedTheme = localStorage.getItem('theme');
        const systemTheme = window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
        const theme = savedTheme || systemTheme;
        
        document.documentElement.setAttribute('data-theme', theme);
        updateThemeToggle(theme);
      }

      function toggleTheme() {
        const currentTheme = document.documentElement.getAttribute('data-theme');
        const newTheme = currentTheme === 'dark' ? 'light' : 'dark';
        
        document.documentElement.setAttribute('data-theme', newTheme);
        localStorage.setItem('theme', newTheme);
        updateThemeToggle(newTheme);
      }

      function updateThemeToggle(theme) {
        const toggle = document.getElementById('theme-toggle');
        if (toggle) {
          toggle.textContent = theme === 'dark' ? '☀️' : '🌙';
          toggle.title = `Switch to ${theme === 'dark' ? 'light' : 'dark'} mode`;
        }
      }

      // Initialize theme on page load
      document.addEventListener('DOMContentLoaded', initTheme);

      // Listen for system theme changes
      window.matchMedia('(prefers-color-scheme: dark)').addEventListener('change', (e) => {
        if (!localStorage.getItem('theme')) {
          const theme = e.matches ? 'dark' : 'light';
          document.documentElement.setAttribute('data-theme', theme);
          updateThemeToggle(theme);
        }
      });

      // Auto-refresh the page every 30 seconds to show updated client status
      function startAutoRefresh() {
        if (window.location.pathname === '/') {
          setInterval(() => {
            // Only refresh if user hasn't interacted recently (no scrolling, clicking, etc.)
            if (document.hidden || Date.now() - window.lastUserActivity > 10000) {
              return; // Don't refresh if page is hidden or user was recently active
            }
            
            // Smooth refresh without disturbing user
            fetch(window.location.href)
              .then(response => response.text())
              .then(html => {
                const parser = new DOMParser();
                const newDoc = parser.parseFromString(html, 'text/html');
                
                // Update only the stats and client cards
                const currentStats = document.querySelector('.stats');
                const newStats = newDoc.querySelector('.stats');
                if (currentStats && newStats) {
                  currentStats.innerHTML = newStats.innerHTML;
                }
                
                const currentGrid = document.querySelector('.grid');
                const newGrid = newDoc.querySelector('.grid');
                if (currentGrid && newGrid) {
                  currentGrid.innerHTML = newGrid.innerHTML;
                }
              })
              .catch(err => console.log('Auto-refresh failed:', err));
          }, 30000); // Refresh every 30 seconds
        }
      }

      // Track user activity
      window.lastUserActivity = Date.now();
      ['mousedown', 'mousemove', 'keypress', 'scroll', 'touchstart'].forEach(event => {
        document.addEventListener(event, () => {
          window.lastUserActivity = Date.now();
        }, { passive: true });
      });

      // Start auto-refresh on page load
      document.addEventListener('DOMContentLoaded', startAutoRefresh);
    </script>
  </head>

  <body>
    <header class="header">
      <div class="container">
        <div id="theme-toggle" class="theme-toggle" onclick="toggleTheme()" title="Toggle theme">🌙</div>
        <a href="/" style="text-decoration: none; color: inherit; display: block;">
          <div class="header-content">
            <div class="header-icon">
              <img src="/mom_transparent.png" alt="MoM Logo" />
            </div>
            <div class="header-text">
              <h1 class="brand-name">MoM</h1>
              <div class="brand-subtitle">Mule of Mules</div>
              <p class="tagline">Centralized Management for Mule AI Agents</p>
            </div>
          </div>
        </a>
      </div>
    </header>

    <main class="main-content">
      <div class="container">
        <% if notice %>
          <div class="alert alert-success"><%= notice %></div>
        <% end %>
        
        <% if alert %>
          <div class="alert alert-error"><%= alert %></div>
        <% end %>
        
        <%= yield %>
      </div>
    </main>
  </body>
</html>